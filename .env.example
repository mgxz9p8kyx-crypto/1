# Admin Authentication
# Email for admin dashboard login
ADMIN_EMAIL=your-admin-email@example.com

# Base64-encoded password for admin login
# To generate: echo -n "your-password" | base64
ADMIN_PASSWORD_HASH=base64-encoded-password

# SendGrid Configuration
# Get API key from https://sendgrid.com (Settings > API Keys)
SENDGRID_API_KEY=your-sendgrid-api-key

# Email addresses
# Sender email (must be verified in SendGrid)
FROM_EMAIL=noreply@qabyotire.com

# Recipient email for contact form submissions
CONTACT_EMAIL=qabyotire99@gmail.com
